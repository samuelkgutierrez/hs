app (
    abs (b:Nat .
        app (
            abs (a:Nat . if iszero a
                            then true
                            else abs (b:Nat . b
                         fi
            )
            , 0
        )
    )
    , 0
)
